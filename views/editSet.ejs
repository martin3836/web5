<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit LEGO Set</title>
    <link rel="stylesheet" href="/css/main.css">
</head>
<body class="bg-gray-100">
<%- include('partials/navbar', {page: '/lego/editSet'}) %>

<div class="container mx-auto px-4 py-5">
    <div class="bg-white shadow-lg rounded-lg p-6">
        <h1 class="text-xl font-semibold mb-4">Edit Set - <%= set.name %></h1>
        <form action="/lego/editSet" method="POST" class="space-y-4">
            <!-- Hidden input for set_num -->
            <input type="hidden" name="set_num" value="<%= set.set_num %>">

            <div class="p-3 rounded-lg bg-blue-50">
                <label for="name" class="block text-sm font-medium text-gray-700">Name (Click to edit):</label>
                <input type="text" id="name" name="name" value="<%= set.name %>" required class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-300 focus:ring focus:ring-indigo-200 focus:ring-opacity-50">
            </div>
            <div class="p-3 rounded-lg bg-blue-50">
                <label for="year" class="block text-sm font-medium text-gray-700">Year (Click to edit):</label>
                <input type="number" id="year" name="year" value="<%= set.year %>" required class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-300 focus:ring focus:ring-indigo-200 focus:ring-opacity-50">
            </div>
            <div class="p-3 rounded-lg bg-blue-50">
                <label for="num_parts" class="block text-sm font-medium text-gray-700">Number of Parts (Click to edit):</label>
                <input type="number" id="num_parts" name="num_parts" value="<%= set.num_parts %>" required class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-300 focus:ring focus:ring-indigo-200 focus:ring-opacity-50">
            </div>
            <div class="p-3 rounded-lg bg-blue-50">
                <label for="img_url" class="block text-sm font-medium text-gray-700">Image URL (Click to edit):</label>
                <input type="url" id="img_url" name="img_url" value="<%= set.img_url %>" class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-300 focus:ring focus:ring-indigo-200 focus:ring-opacity-50">
            </div>
            <div class="p-3 rounded-lg bg-blue-50">
                <label for="theme_id" class="block text-sm font-medium text-gray-700">Theme (Click to edit):</label>
                <select id="theme_id" name="theme_id" required class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-300 focus:ring focus:ring-indigo-200 focus:ring-opacity-50">
                    <% themes.forEach(theme => { %>
                        <option value="<%= theme.id %>" <%= set.theme_id == theme.id ? 'selected' : '' %>><%= theme.name %></option>
                    <% }) %>
                </select>
            </div>
            <div class="flex items-center justify-between">
                <button type="submit" class="btn btn-primary">Update Set</button>
                <a href="/lego/deleteSet/<%= set.set_num %>" class="btn btn-error">Delete Set</a>
                <button type="button" class="btn btn-secondary mt-4" onclick="location.href='/lego/sets'">Return to Sets</button>
            </div>
        </form>
    </div>
</div>
</body>
</html>
